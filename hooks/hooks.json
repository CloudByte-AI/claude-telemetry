{
  "version": 1,
  "hooks": {
    "sessionStart": [
      {
        "command": "uv run --directory \"${CURSOR_PLUGIN_ROOT}\" -m src.cursor.main session_start",
        "type": "command",
        "timeout": 30
      }
    ]
  }
}
